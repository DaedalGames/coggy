# Rust build output
/target/
**/*.rs.bk
*.pdb

# Bench output. Committed results live under docs/measurements/ deliberately.
/bench-out/

# Agent tooling
.claude/
.codegraph/
.omo/
.re0/

# Other toolchains
node_modules/

# Local scratch and OS noise
*.local*
*.log
.DS_Store

# wait-for-quiet's PID lock. Committing one would refuse a fresh clone's
# first waiter until the stale pid happened to be dead.
.wait-for-quiet.lock
