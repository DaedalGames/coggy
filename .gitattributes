# Normalize to LF in the repository. Windows checkouts still get CRLF locally
# where the platform wants it; this only fixes what is stored.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.pdf binary
